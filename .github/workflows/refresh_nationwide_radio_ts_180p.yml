name: Legacy nationwide radio TS builder

# Superseded by refresh_radio_ts_180p.yml, which now covers
# nationwide Radiko, all NHK radio entries, and FreeWiFi compact radio.
on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by Refresh all radio TS visuals'
