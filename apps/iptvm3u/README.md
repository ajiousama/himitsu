# IPTVM3U app subsystem

Build inputs and generated app-specific EPG for the patched standalone M3U IPTV client.

- `build_epg.py` -> `apps/iptvm3u/iptvm3u_epg.xml`
- `build/` contains APK patch/signing inputs
- root `iptvm3u_epg.xml`, `iptvm3u.apk`, and `a.apk` remain compatibility/public mirrors
