# Jun IPTV app subsystem

The build itself is produced from the `jun-iptv-build` branch. This directory documents ownership on main.

- root `j.apk` remains the public compatibility mirror
- Vercel `/j.apk` continues redirecting to the latest build artifact
